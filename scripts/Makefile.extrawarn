# SPDX-License-Identifier: GPL-2.0
# ==========================================================================
# make W=... settings
#
# There are three warning groups enabled by W=1, W=2, W=3.
# They are independent, and can be combined like W=12 or W=123.
# ==========================================================================

KBUILD_CFLAGS += $(call cc-disable-warning, packed-not-aligned)

# backward compatibility
KBUILD_EXTRA_WARN ?= $(KBUILD_ENABLE_EXTRA_GCC_CHECKS)

ifeq ("$(origin W)", "command line")
  KBUILD_EXTRA_WARN := $(W)
endif

ifeq ("$(origin E)", "command line")
  KBUILD_EXTRA_ERR := $(E)
endif

export KBUILD_EXTRA_WARN
export KBUILD_EXTRA_ERR

#
# W=1 - warnings which may be relevant and do not occur too often
#
ifneq ($(findstring 1, $(KBUILD_EXTRA_WARN)),)

KBUILD_CPPFLAGS += -DKBUILD_EXTRA_WARN1

endif

#
# W=2 - warnings which occur quite often but may still be relevant
#
ifneq ($(findstring 2, $(KBUILD_EXTRA_WARN)),)

KBUILD_CPPFLAGS += -DKBUILD_EXTRA_WARN2

endif

#
# W=3 - more obscure warnings, can most likely be ignored
#
ifneq ($(findstring 3, $(KBUILD_EXTRA_WARN)),)

KBUILD_CPPFLAGS += -DKBUILD_EXTRA_WARN3

endif

ifneq ($(findstring 0, $(KBUILD_EXTRA_ERR)),)

KBUILD_CPPFLAGS += -DKBUILD_ERR0

endif

ifneq ($(findstring 1, $(KBUILD_EXTRA_ERR)),)

KBUILD_CPPFLAGS += -DKBUILD_ERR1

endif

ifneq ($(findstring 2, $(KBUILD_EXTRA_ERR)),)

KBUILD_CPPFLAGS += -DKBUILD_ERR2

endif

ifneq ($(findstring 3, $(KBUILD_EXTRA_ERR)),)

KBUILD_CPPFLAGS += -DKBUILD_ERR3

endif
