config SCSI_ESAS2R
	tristate "ATTO Technology's ExpressSAS RAID adapter driver"
	depends on PCI && SCSI
	depends on Y2038_UNSAFE
	---help---
	  This driver supports the ATTO ExpressSAS R6xx SAS/SATA RAID controllers.
